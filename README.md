# Meditronica (Vite + React + TS)

This project replicates the layout of the provided reference page and is scaffolded to add pages incrementally.

## Run locally
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
npm run preview
```
# meditronica
